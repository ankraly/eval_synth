folder containing paired up audio files, with each one containing
TARGET and SYSTEM separated by some settable silence

every filename must end with speaker ID, whatever that may be, split
from filename stem with an underscore
e.g. b_a_AK_and_b_a_baseline , ou_AK_and_ou_baseline
